<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlantUMLEditor"
    xmlns:models="clr-namespace:PlantUMLEditor.Models"
    xmlns:localControls="clr-namespace:PlantUMLEditor.Controls"
   xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors" x:Class="PlantUMLEditor.MainWindow"
    xmlns:behaviors="clr-namespace:PlantUMLEditor.Behaviors"
    xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"

        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Aaron's Plant UML Editor" Height="{Binding UIModel.WindowHeight, Mode=TwoWay, FallbackValue=850}" Width="{Binding UIModel.WindowWidth, Mode=TwoWay, FallbackValue=1000}"
FontFamily="Segoe UI"  Loaded="Window_Loaded"
    Left="{Binding UIModel.WindowLeft, Mode=TwoWay}"
    Top="{Binding UIModel.WindowTop, Mode=TwoWay}"
    xmlns:System="clr-namespace:System;assembly=System.Runtime" xmlns:componentmodel="clr-namespace:System.ComponentModel;assembly=WindowsBase" xmlns:svgc="http://sharpvectors.codeplex.com/svgc/" Icon="/PlantUMLEditor;component/Images/uml.png">

<Window.CommandBindings>
        <CommandBinding Command="{x:Static markdig:Commands.Hyperlink}" Executed="HyperlinkExecuted" />
</Window.CommandBindings>
    <b:Interaction.Triggers>

        <b:EventTrigger EventName="SizeChanged">
            <b:CallMethodAction MethodName="UISizeChanged" TargetObject="{Binding UIModel}"></b:CallMethodAction>

        </b:EventTrigger>

    </b:Interaction.Triggers>

    <Window.Resources>
        <CollectionViewSource x:Key="GroupedMessages" IsLiveSortingRequested="True" Source="{Binding Messages}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Warning"  />
            </CollectionViewSource.GroupDescriptions>
            <CollectionViewSource.LiveSortingProperties>
                <System:String>SortOrder</System:String>
            </CollectionViewSource.LiveSortingProperties>
            <CollectionViewSource.SortDescriptions>

                <componentmodel:SortDescription PropertyName="SortOrder" Direction="Ascending" />
                <componentmodel:SortDescription PropertyName="Warning" Direction="Ascending" />
               
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        <SolidColorBrush x:Key="CustomColor" Color="#FF4D8DEA"></SolidColorBrush>
        
        <!-- Modern Color Palette -->
        <SolidColorBrush x:Key="PrimaryAccent" Color="#007ACC"/>
        <SolidColorBrush x:Key="PrimaryAccentHover" Color="#1E8AD6"/>
        <SolidColorBrush x:Key="BackgroundSecondary" Color="#F3F3F3"/>
        <SolidColorBrush x:Key="BackgroundHover" Color="#E8E8E8"/>
        <SolidColorBrush x:Key="BorderSubtle" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#616161"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#28A745"/>
        <SolidColorBrush x:Key="WarningColor" Color="#FF9500"/>
        
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="{StaticResource BackgroundSecondary}"/>
        </Style>

        <!-- Modern MenuItem Styles -->
        <Style TargetType="MenuItem">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#1F1F1F"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundHover}"/>
                </Trigger>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{StaticResource BackgroundHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1F1F1F"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <ContextMenu x:Key="FolderMenu">
            <MenuItem Header="New Class Diagram" Command="{Binding DataContext.CreateNewClassDiagram, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New Sequence Diagram" Command="{Binding DataContext.CreateNewSequenceDiagram , RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New Component Diagram" Command="{Binding DataContext.CreateNewComponentDiagram, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New UML Document" Command="{Binding DataContext.CreateNewUnknownDiagram, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New JSON UML Document" Command="{Binding DataContext.CreateNewJSONDocumentCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New Markdown Document" Command="{Binding DataContext.CreateMarkDownDocument, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New YAML Document" Command="{Binding DataContext.CreateYAMLDocument, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <MenuItem Header="New URL Link" Command="{Binding DataContext.CreateNewURLLinkCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <Separator></Separator>

            <MenuItem Header="New Folder" Command="{Binding NewFolderCommand}"></MenuItem>
            <MenuItem Header="Rename" Command="{Binding StartRenameCommand}"></MenuItem>
            <Separator>

            </Separator>
            <MenuItem Header="Delete" Command="{Binding DeleteCommand}"></MenuItem>
        </ContextMenu>
        <ContextMenu x:Key="FileMenuUML">

            <MenuItem Header="Rename" Command="{Binding StartRenameCommand}"></MenuItem>
            <Separator></Separator>
            <MenuItem Header="Delete" Command="{Binding DeleteCommand}"></MenuItem>
            <Separator></Separator>
            <MenuItem Header="Generate PNG Image" Command="{Binding DataContext.CreateUMLPngImage, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <Separator></Separator>
            <MenuItem Header="Generate SVG Image" Command="{Binding DataContext.CreateUMLSVGImage, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}" ></MenuItem>
            <Separator></Separator>
            <MenuItem Header="Undo Git Changes" Command="{Binding DataContext.UndoGitChangesCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" CommandParameter="{Binding}" />

        </ContextMenu>
        <ContextMenu x:Key="FileMenu">
            <MenuItem Header="Open In Native App" Command="{Binding OpenInNativeCommand}"></MenuItem>
            <MenuItem Header="Rename" Command="{Binding StartRenameCommand}"></MenuItem>
            <Separator></Separator>
            <MenuItem Header="Delete" Command="{Binding DeleteCommand}"></MenuItem>
            <Separator></Separator>
            <MenuItem Header="Undo Git Changes" Command="{Binding DataContext.UndoGitChangesCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" CommandParameter="{Binding}" />

        </ContextMenu>
        <System:Double x:Key="AutoCompleteOpacity">0.8</System:Double>
        <DataTemplate DataType="{x:Type models:SVGDocumentModel}">

            <Grid Height="{Binding ActualHeight, ElementName=Tabs}" Visibility="{Binding Visible}">

                <ScrollViewer Grid.Row="2" Padding="10" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PanningMode="Both" Background="{StaticResource BackgroundSecondary}">
                    <Behaviors:Interaction.Behaviors>
                        <Behaviors:TranslateZoomRotateBehavior />
                    </Behaviors:Interaction.Behaviors>
                    <localControls:PanAndZoomBorder>
                        <svgc:SvgViewbox       Stretch="Uniform"
                      Source="{Binding SVG}">
                            <svgc:SvgViewbox.LayoutTransform>
                                <ScaleTransform x:Name="SvgScale" ScaleX="1" ScaleY="1"/>
                            </svgc:SvgViewbox.LayoutTransform>



                        </svgc:SvgViewbox>
                    </localControls:PanAndZoomBorder>

                </ScrollViewer>

       
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:ImageDocumentModel}">

            <Grid Height="{Binding ActualHeight, ElementName=Tabs}" Visibility="{Binding Visible}">
                <Border BorderThickness="1" CornerRadius="6" Margin="4" BorderBrush="{StaticResource BorderSubtle}">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.1"/>
                    </Border.Effect>
                    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto"  VerticalScrollBarVisibility="Auto" PanningMode="Both">
                        <Behaviors:Interaction.Behaviors>
                            <Behaviors:TranslateZoomRotateBehavior />
                        </Behaviors:Interaction.Behaviors>
                        <localControls:PanAndZoomBorder>
                            <Image Source="{Binding Image}" Stretch="None" ></Image>
                        </localControls:PanAndZoomBorder>
                    </ScrollViewer>
                </Border>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:UrlLinkDocumentModel}">

            <Grid Height="{Binding ActualHeight, ElementName=Tabs}" Visibility="{Binding Visible}">
                <Border BorderThickness="1" CornerRadius="6" Margin="4" BorderBrush="{StaticResource BorderSubtle}">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.1"/>
                    </Border.Effect>
                    <wv2:WebView2
                    Source="{Binding URL}"
                    
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    >
                        <b:Interaction.Behaviors>
                            <behaviors:WebView2HtmlCaptureBehavior />
                        </b:Interaction.Behaviors>
                    </wv2:WebView2>

                </Border>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:TextDocumentModel}">
            <Grid Height="{Binding ActualHeight, ElementName=Tabs}" Visibility="{Binding Visible}">
                <Border BorderThickness="1" CornerRadius="6" Margin="4">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.1"/>
                    </Border.Effect>
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDirty}" Value="true">
                                    <DataTrigger.Setters>
                                        <Setter Property="BorderBrush" Value="{StaticResource WarningColor}"/>
                                    </DataTrigger.Setters>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsDirty}" Value="false">
                                    <DataTrigger.Setters>
                                        <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                                    </DataTrigger.Setters>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="2*"></ColumnDefinition>

                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>

                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="5">
                            </RowDefinition>
                            <RowDefinition  Height="{Binding FindHeight,  ElementName=Editor}" ></RowDefinition>
                        </Grid.RowDefinitions>
                        <Image Source="{Binding Children[1].LineNumbers, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Grid}}}" ></Image>
                        <localControls:MyTextBox Grid.Column="1" IsInactiveSelectionHighlightEnabled="True" AllowDrop="True"
                                              UndoLimit="100" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                            DataContext="{Binding}"   HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
 Style="{DynamicResource TextBoxStyle1}" x:Name="Editor"
                                                 CaretBrush="Green"
                            AcceptsReturn="True"  AcceptsTab="True"
                                                
                                                 FindAllReferencesCommand="{Binding DataContext.FindAllReferencesCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                                 GotoDefinitionCommand="{Binding DataContext.GotoDefinitionCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                               FontFamily="Cascadia Code" FontSize="{Binding DataContext.EditorFontSize, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"  >
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="PreviewDrop">
                                    <b:CallMethodAction MethodName="TextDrop" TargetObject="{Binding DataContext,
                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                                <b:EventTrigger EventName="PreviewDragOver">
                                    <b:CallMethodAction MethodName="TextDragOver" TargetObject="{Binding DataContext,
                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                            <localControls:MyTextBox.PopupControl>
                                <Popup>

                                    <Grid x:Name="AutoCompleteGrid">
                                        <Grid.RowDefinitions>

                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>

                                        <ListBox x:Name="AutoCompleteBox" Grid.Row="2" Grid.ColumnSpan="2"
                             MaxHeight="100"
                           ItemsSource="{Binding SortedMatchingAutoCompletes}">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"></TextBlock>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Grid>
                                </Popup>
                            </localControls:MyTextBox.PopupControl>
                        </localControls:MyTextBox>



                        <GridSplitter Grid.Row="1" Grid.Column="1" ShowsPreview="True" ResizeDirection="Rows"  HorizontalAlignment="Stretch" Cursor="SizeNS"></GridSplitter>

                        <Border Grid.Row="2" Grid.Column="1" BorderThickness="1" BorderBrush="{StaticResource BorderSubtle}"
                                CornerRadius="4" Margin="4" Background="{StaticResource BackgroundSecondary}"
                                Width="{Binding Width, ElementName=Editor}">

                            <Grid Width="{Binding ActualWidth, ElementName=Editor}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200" ></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="200" ></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition></RowDefinition>
                                </Grid.RowDefinitions>


                                <TextBox Grid.Row="0" Margin="{DynamicResource TabHeaderMargin}" VerticalContentAlignment="Center"  Text="{Binding FindText, Mode=TwoWay, ElementName=Editor}">
                                    <b:Interaction.Triggers>
                                        <b:KeyTrigger Key="Return">
                                            <b:InvokeCommandAction Command="{Binding FindCommand, ElementName=Editor, UpdateSourceTrigger=PropertyChanged}"></b:InvokeCommandAction>
                                        </b:KeyTrigger>
                                    </b:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Margin="{DynamicResource TabHeaderMargin}" Command="{Binding FindCommand, ElementName=Editor}" Content="Find" ></Button>
                                <CheckBox Grid.Column="2" VerticalAlignment="Center"   Margin="{DynamicResource TabHeaderMargin}" IsChecked="{Binding UseRegex, ElementName=Editor}">Use Regex</CheckBox>
                                <TextBox Grid.Row="0" VerticalContentAlignment="Center" Margin="{DynamicResource TabHeaderMargin}"   Grid.Column="3" Text="{Binding ReplaceText, Mode=TwoWay, ElementName=Editor, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                                <Button Grid.Column="4"  Margin="{DynamicResource TabHeaderMargin}" Command="{Binding ReplaceCommand, ElementName=Editor}" Content="Replace"  ></Button>
                                <Button Grid.Column="5" Margin="{DynamicResource TabHeaderMargin}" Command="{Binding ClearCommand, ElementName=Editor}" Content="Clear"  ></Button>

                                <DataGrid CanUserAddRows="false"  ItemsSource="{Binding FindResults, ElementName=Editor}" Grid.Row="1" Grid.ColumnSpan="7" MaxHeight="300"
                                 AutoGenerateColumns="False" VerticalContentAlignment="Center"     SelectedItem="{Binding SelectedFindResult, Mode=TwoWay, ElementName=Editor}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Line"  IsReadOnly="true"  Binding="{Binding LineNumber}" ></DataGridTextColumn>
                                        <DataGridTextColumn Header="Match" IsReadOnly="true"  Binding="{Binding Line}" ></DataGridTextColumn>
                                        <DataGridTextColumn Header="Replace" IsReadOnly="true" Binding="{Binding ReplacePreview}" ></DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type models:MDDocumentModel}">
            <Grid Height="{Binding ActualHeight, ElementName=Tabs}" Visibility="{Binding Visible}">
                <Border BorderThickness="1" CornerRadius="6" Margin="4">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.1"/>
                    </Border.Effect>
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDirty}" Value="true">
                                    <DataTrigger.Setters>
                                        <Setter Property="BorderBrush" Value="{StaticResource WarningColor}"/>
                                    </DataTrigger.Setters>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsDirty}" Value="false">
                                    <DataTrigger.Setters>
                                        <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                                    </DataTrigger.Setters>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>

                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="5">
                            </RowDefinition>
                            <RowDefinition  Height="{Binding FindHeight,  ElementName=Editor}" ></RowDefinition>
                        </Grid.RowDefinitions>
                        <Image Source="{Binding Children[1].LineNumbers, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Grid}}}" ></Image>
                        <localControls:MyTextBox Grid.Column="1" IsInactiveSelectionHighlightEnabled="True" AllowDrop="True"
                                             UndoLimit="100" ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                           DataContext="{Binding}"   HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
Style="{DynamicResource TextBoxStyle1}" x:Name="Editor"
                                            SpellCheck.IsEnabled="{Binding DataContext.SpellCheck,  RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                                CaretBrush="Green"
                           AcceptsReturn="True"  AcceptsTab="True"
                                                FindAllReferencesCommand="{Binding DataContext.FindAllReferencesCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                                GotoDefinitionCommand="{Binding DataContext.GotoDefinitionCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                              FontFamily="Cascadia Code" FontSize="14">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="PreviewDrop">
                                    <b:CallMethodAction MethodName="TextDrop" TargetObject="{Binding DataContext,
                                       RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                                <b:EventTrigger EventName="PreviewDragOver">
                                    <b:CallMethodAction MethodName="TextDragOver" TargetObject="{Binding DataContext,
                                       RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                            <localControls:MyTextBox.PopupControl>
                                <Popup>

                                    <Grid x:Name="AutoCompleteGrid">
                                        <Grid.RowDefinitions>

                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                            <RowDefinition Height="Auto"></RowDefinition>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>

                                        <ListBox x:Name="AutoCompleteBox" Grid.Row="2" Grid.ColumnSpan="2"
                            MaxHeight="100"
                          ItemsSource="{Binding SortedMatchingAutoCompletes}">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"></TextBlock>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </Grid>
                                </Popup>
                            </localControls:MyTextBox.PopupControl>
                        </localControls:MyTextBox>

                        <GridSplitter Grid.Row="1" Grid.Column="1" ShowsPreview="True" ResizeDirection="Rows"  HorizontalAlignment="Stretch" Cursor="SizeNS"></GridSplitter>

                        <Border Grid.Row="2" Grid.Column="1" BorderThickness="1" BorderBrush="Black"
                               Width="{Binding Width, ElementName=Editor}">

                            <Grid Width="{Binding ActualWidth, ElementName=Editor}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200" ></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="200" ></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition></RowDefinition>
                                </Grid.RowDefinitions>


                                <TextBox Grid.Row="0" Margin="{DynamicResource TabHeaderMargin}" VerticalContentAlignment="Center"  Text="{Binding FindText, Mode=TwoWay, ElementName=Editor}">
                                    <b:Interaction.Triggers>
                                        <b:KeyTrigger Key="Return">
                                            <b:InvokeCommandAction Command="{Binding FindCommand, ElementName=Editor, UpdateSourceTrigger=PropertyChanged}"></b:InvokeCommandAction>
                                        </b:KeyTrigger>
                                    </b:Interaction.Triggers>
                                </TextBox>
                                <Button Grid.Column="1" Margin="{DynamicResource TabHeaderMargin}" Command="{Binding FindCommand, ElementName=Editor}" Content="Find" ></Button>
                                <CheckBox Grid.Column="2" VerticalAlignment="Center"   Margin="{DynamicResource TabHeaderMargin}" IsChecked="{Binding UseRegex, ElementName=Editor}">Use Regex</CheckBox>
                                <TextBox Grid.Row="0" VerticalContentAlignment="Center" Margin="{DynamicResource TabHeaderMargin}"   Grid.Column="3" Text="{Binding ReplaceText, Mode=TwoWay, ElementName=Editor, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                                <Button Grid.Column="4"  Margin="{DynamicResource TabHeaderMargin}" Command="{Binding ReplaceCommand, ElementName=Editor}" Content="Replace"  ></Button>
                                <Button Grid.Column="5" Margin="{DynamicResource TabHeaderMargin}" Command="{Binding ClearCommand, ElementName=Editor}" Content="Clear"  ></Button>

                                <DataGrid CanUserAddRows="false"  ItemsSource="{Binding FindResults, ElementName=Editor}" Grid.Row="1" Grid.ColumnSpan="7" MaxHeight="300"
                                AutoGenerateColumns="False" VerticalContentAlignment="Center"     SelectedItem="{Binding SelectedFindResult, Mode=TwoWay, ElementName=Editor}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Line"  IsReadOnly="true"  Binding="{Binding LineNumber}" ></DataGridTextColumn>
                                        <DataGridTextColumn Header="Match" IsReadOnly="true"  Binding="{Binding Line}" ></DataGridTextColumn>
                                        <DataGridTextColumn Header="Replace" IsReadOnly="true" Binding="{Binding ReplacePreview}" ></DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </DataTemplate>
        <HierarchicalDataTemplate  DataType="{x:Type  models:TreeViewModel}" ItemsSource="{Binding Children}">

            <StackPanel Orientation="Horizontal">



                <Image Source="{Binding Icon}" Width="15" Margin="1,0,5,0">
                </Image>
                <TextBox x:Name="rename" Text="{Binding Rename, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <b:Interaction.Triggers>
                        <b:KeyTrigger Key="Return" ActiveOnFocus="True">

                            <b:InvokeCommandAction  Command="{Binding  DoRenameCommand}"></b:InvokeCommandAction>
                        </b:KeyTrigger>

                        <b:EventTrigger EventName="LostFocus">
                            <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="IsRenaming" Value="false"></b:ChangePropertyAction>

                        </b:EventTrigger>
                        <b:KeyTrigger Key="Esc" ActiveOnFocus="True">
                            <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="IsRenaming" Value="false"></b:ChangePropertyAction>
                        </b:KeyTrigger>
                    </b:Interaction.Triggers>
                </TextBox>

                <TextBlock x:Name="txt" Text="{Binding Name}" Width="Auto" FontWeight="Bold">

                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
              
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasMessages}" Value="true">


                                    <DataTrigger.Setters>
                                        <Setter   Property="Foreground" Value="#FFAA0000">
                                        </Setter>
                                    </DataTrigger.Setters>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding HasMessages}" Value="false">


                                    <DataTrigger.Setters>
                                        <Setter   Property="Foreground" Value="Black">
                                        </Setter>
                                    </DataTrigger.Setters>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

            </StackPanel>

            <HierarchicalDataTemplate.Triggers>
                <DataTrigger Value="true">
                    <DataTrigger.Binding>
                        <Binding  Path="IsRenaming"></Binding>
                    </DataTrigger.Binding>
                    <DataTrigger.Setters>
                        <Setter Property="FocusManager.FocusedElement" TargetName="rename" Value="{Binding ElementName=rename}"></Setter>

                        <Setter Property="Visibility"  TargetName="rename" Value="Visible">
                        </Setter>
                        <Setter Property="Visibility"  TargetName="txt" Value="Collapsed">
                        </Setter>
                    </DataTrigger.Setters>
                </DataTrigger>
                <DataTrigger Value="false">
                    <DataTrigger.Binding>
                        <Binding  Path="IsRenaming"></Binding>
                    </DataTrigger.Binding>
                    <DataTrigger.Setters>

                        <Setter Property="Visibility"  TargetName="rename" Value="Collapsed">
                        </Setter>
                        <Setter Property="Visibility"  TargetName="txt" Value="Visible">
                        </Setter>
                    </DataTrigger.Setters>
                </DataTrigger>
            </HierarchicalDataTemplate.Triggers>
        </HierarchicalDataTemplate>
        <local:TabHeaderDataTemplateSelector x:Key="TabHeaderSelector"></local:TabHeaderDataTemplateSelector>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" SnapsToDevicePixels="true" StrokeThickness="1" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <SolidColorBrush x:Key="ListBorder" Color="#828790" />
        <Style x:Key="TreeViewStyle1" TargetType="{x:Type TreeView}">
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
            <Setter Property="BorderBrush" Value="{StaticResource ListBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.PanningMode" Value="Both" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeView}">
                        <Border x:Name="Bd" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" SnapsToDevicePixels="true">
                            <ScrollViewer x:Name="_tv_scrollviewer_" Background="{TemplateBinding Background}" CanContentScroll="false" Focusable="false" HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                            </Trigger>
                            <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
                                <Setter Property="CanContentScroll" TargetName="_tv_scrollviewer_" Value="true" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Stroke" Color="#FF818181" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Fill" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Stroke" Color="#FF27C7F7" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Fill" Color="#FFCCEEFB" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Checked.Stroke" Color="#FF262626" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.Static.Checked.Fill" Color="#FF595959" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Checked.Stroke" Color="#FF1CC4F7" />
        <SolidColorBrush x:Key="TreeViewItem.TreeArrow.MouseOver.Checked.Fill" Color="#FF82DFFB" />
        <PathGeometry x:Key="TreeArrow" Figures="M0,0 L0,6 L6,0 z" />
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Width" Value="16" />
            <Setter Property="Height" Value="16" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border Background="Transparent" Height="16" Padding="5,5,5,5" Width="16">
                            <Path x:Name="ExpandPath" Data="{StaticResource TreeArrow}" Fill="{StaticResource TreeViewItem.TreeArrow.Static.Fill}" Stroke="{StaticResource TreeViewItem.TreeArrow.Static.Stroke}">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="135" CenterY="3" CenterX="3" />
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="RenderTransform" TargetName="ExpandPath">
                                    <Setter.Value>
                                        <RotateTransform Angle="180" CenterY="3" CenterX="3" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Fill" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.Static.Checked.Fill}" />
                                <Setter Property="Stroke" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.Static.Checked.Stroke}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Stroke" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Stroke}" />
                                <Setter Property="Fill" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Fill}" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsChecked" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Stroke" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Checked.Stroke}" />
                                <Setter Property="Fill" TargetName="ExpandPath" Value="{StaticResource TreeViewItem.TreeArrow.MouseOver.Checked.Fill}" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TreeViewItemFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style   TargetType="{x:Type TreeViewItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="1,0,0,0" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource TreeViewItemFocusVisual}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid Margin="-5,0,0,0">

                            <b:Interaction.Triggers>

                                <b:EventTrigger EventName="MouseMove">
                                    <b:CallMethodAction MethodName="TreeMouseMove" TargetObject="{Binding Path=DataContext,  RelativeSource={RelativeSource FindAncestor,
AncestorType={x:Type TreeView}}}"></b:CallMethodAction>
                                </b:EventTrigger>

                                <b:EventTrigger EventName="Drop">
                                    <b:CallMethodAction MethodName="TreeDrop" TargetObject="{Binding DataContext,
                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                                <b:EventTrigger EventName="DragOver">
                                    <b:CallMethodAction MethodName="TreeDragOver" TargetObject="{Binding DataContext,
                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"></b:CallMethodAction>
                                </b:EventTrigger>


                                <b:EventTrigger EventName="MouseLeftButtonUp">
                                    <b:CallMethodAction MethodName="TreeItemClickedButtonUp" TargetObject="{Binding Path=DataContext,  RelativeSource={RelativeSource FindAncestor,
AncestorType={x:Type TreeView}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                                <b:EventTrigger EventName="MouseLeftButtonDown">
                                    <b:CallMethodAction MethodName="TreeItemClickedButtonDown" TargetObject="{Binding Path=DataContext,  RelativeSource={RelativeSource FindAncestor,
AncestorType={x:Type TreeView}}}"></b:CallMethodAction>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition MinWidth="19" Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="Expander" ClickMode="Press" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ExpandCollapseToggleStyle}" />
                            <Border x:Name="Bd" CornerRadius="4" HorizontalAlignment="Stretch"  Background="{TemplateBinding Background}" BorderThickness="0" BorderBrush="{TemplateBinding BorderBrush}" Grid.Column="1" Padding="4,3" SnapsToDevicePixels="true">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Rectangle Grid.Column="0" RadiusX="2" RadiusY="2">
                                        <Rectangle.Style>
                                            <Style TargetType="Rectangle">
                                                <Setter Property="Fill" Value="Transparent" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding GitStatus}" Value="{x:Static models:GitFileStatus.Modified}">
                                                        <Setter Property="Fill" Value="#FFFFA500" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding GitStatus}" Value="{x:Static models:GitFileStatus.Untracked}">
                                                        <Setter Property="Fill" Value="#FF00C800" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding GitStatus}" Value="{x:Static models:GitFileStatus.Deleted}">
                                                        <Setter Property="Fill" Value="#FFFF0000" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding GitStatus}" Value="{x:Static models:GitFileStatus.Staged}">
                                                        <Setter Property="Fill" Value="#FF0096FF" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding GitStatus}" Value="{x:Static models:GitFileStatus.Conflict}">
                                                        <Setter Property="Fill" Value="#FFC800C8" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding GitStatus}" Value="{x:Static models:GitFileStatus.Ignored}">
                                                        <Setter Property="Fill" Value="#FF808080" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Rectangle.Style>
                                    </Rectangle>
                                    <ContentPresenter Grid.Column="1" Margin="2,2,2,2" x:Name="PART_Header" ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Grid.ColumnSpan="2" Grid.Column="1" Grid.Row="1" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="false">
                                <Setter Property="Visibility" TargetName="ItemsHost" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Visibility" TargetName="Expander" Value="Hidden" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource PrimaryAccent}" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true" />
                                    <Condition Property="IsSelectionActive" Value="false" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}" />
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}}" />
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsFile}" Value="false">
                    <Setter Property="ContextMenu" Value="{StaticResource FolderMenu}">
                    </Setter>
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay, FallbackValue=False}"></Setter>
                </DataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition  Binding="{Binding IsFile}" Value="true"></Condition>
                        <Condition Binding="{Binding IsUML}" Value="true"></Condition>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="ContextMenu" Value="{StaticResource FileMenuUML}">
                        </Setter>
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition  Binding="{Binding IsFile}" Value="true"></Condition>
                        <Condition Binding="{Binding IsUML}" Value="false"></Condition>
                    </MultiDataTrigger.Conditions>
                    <MultiDataTrigger.Setters>
                        <Setter Property="ContextMenu" Value="{StaticResource FileMenu}">
                        </Setter>
                    </MultiDataTrigger.Setters>
                </MultiDataTrigger>



                <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <SolidColorBrush x:Key="TabItem.Selected.Background" Color="#F0F7FF" />
        <SolidColorBrush x:Key="TabItem.Selected.Border" Color="#007ACC" />
        <SolidColorBrush x:Key="TabItem.Hover.Background" Color="#F5F5F5" />
        <DataTemplate x:Key="TextDocumentTabHeader">

            <Border BorderThickness="0,0,0,3" Margin="4,2,4,0" CornerRadius="6,6,0,0" MinWidth="300">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="2" Opacity="0.08"/>
                </Border.Effect>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource BackgroundSecondary}"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDirty}" Value="true">
                                <DataTrigger.Setters>
                                    <Setter Property="BorderBrush" Value="{StaticResource WarningColor}"/>
                                </DataTrigger.Setters>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDirty}" Value="false">
                                <DataTrigger.Setters>
                                    <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                                </DataTrigger.Setters>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Visible}" Value="0">
                                <DataTrigger.Setters>
                                    <Setter Property="Background" Value="{StaticResource TabItem.Selected.Background}"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryAccent}"/>
                                </DataTrigger.Setters>
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid IsHitTestVisible="True">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Visible}" Value="0">
                                    <Setter Property="Background"  Value="{StaticResource TabItem.Selected.Background}"></Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.ColumnDefinitions>

                        <ColumnDefinition Width="Auto" ></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <Button ToolTip="Save"  Margin="4,4" Grid.Column="0" CommandParameter="{Binding}" Command="{Binding Path=DataContext.SaveCommand, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                            Opacity="0.7">

                        <Button.Content>
                            <Grid>
                                <Image Source="/PlantUMLEditor;component/Images/Save_16x.png"></Image>
                            </Grid>
                        </Button.Content>
                    </Button>
                    <Button ToolTip="Save and Close"  Margin="4,4" Grid.Column="1" CommandParameter="{Binding}" Command="{Binding Path=DataContext.CloseDocumentAndSave, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                            Opacity="0.7">
                        <Button.Content>
                            <Grid>
                                <Image Source="/PlantUMLEditor;component/Images/SaveClose_16x.png"></Image>
                            </Grid>
                        </Button.Content>
                    </Button>

                    <Button ToolTip="Close without saving"  Margin="4,4" Grid.Column="2" CommandParameter="{Binding}" Command="{Binding Path=DataContext.CloseDocument, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}"
                            Opacity="0.7">
                        <Button.Content>
                            <Grid>
                                <Image Source="/PlantUMLEditor;component/Images/Close_8x_16x.png" ></Image>
                            </Grid>
                        </Button.Content>
                    </Button>
                    <Button  ToolTip="Preview" Margin="4,4" Grid.Column="3" 
                                             Command="{Binding  ShowPreviewCommand}"
                            Opacity="0.7">

                        <Button.Content>
                            <Grid>
                                <Image Source="/PlantUMLEditor;component/Images/PreviewTransition_16x.png" ></Image>
                            </Grid>
                        </Button.Content>
                    </Button>
                    <Label 
                            Grid.Column="4" 
                           IsHitTestVisible="True"
                            FontWeight="SemiBold"
                            FontSize="11"
                            VerticalAlignment="Center"  
                            Content="{Binding Title}"
                            Margin="8,4,8,4">
                        <b:Interaction.Triggers>

                            <b:EventTrigger EventName="MouseDown">
                                <b:InvokeCommandAction Command="{Binding DataContext.SelectDocumentCommand, ElementName=Tabs}" 
                            CommandParameter="{Binding}"/>

                            </b:EventTrigger>

                        </b:Interaction.Triggers>

                    </Label>
                </Grid>
            </Border>

        </DataTemplate>
        <DataTemplate x:Key="ImageDocumentTabHeader">

            <Border BorderThickness="0,0,0,3" Margin="4,2,4,0" CornerRadius="6,6,0,0" MinWidth="300" BorderBrush="{StaticResource SuccessColor}">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="2" Opacity="0.08"/>
                </Border.Effect>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{StaticResource BackgroundSecondary}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid IsHitTestVisible="True">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                    <DataTrigger Binding="{Binding Visible}" Value="0">
                                        <Setter Property="Background"  Value="{StaticResource TabItem.Selected.Background}"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.ColumnDefinitions>


                        <ColumnDefinition Width="Auto"></ColumnDefinition>

                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>



                    <Button ToolTip="Close"
                            Margin="{DynamicResource TabHeaderMargin}" 
                            Grid.Column="0"
                            CommandParameter="{Binding}"
                            Command="{Binding Path=DataContext.CloseDocument, Mode=OneWay, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ItemsControl}}}">
                        <Button.Content>
                            <Grid>
                                <Image Source="/PlantUMLEditor;component/Images/Close_8x_16x.png" ></Image>
                            </Grid>
                        </Button.Content>
                    </Button>

                    <Label 
                            Grid.Column="1" 
                           IsHitTestVisible="True"
                            FontWeight="ExtraBold"
                            VerticalAlignment="Center"  
                            Content="{Binding Title}"
                            Margin="{DynamicResource TabHeaderMargin}">
                        <b:Interaction.Triggers>

                            <b:EventTrigger EventName="MouseDown">
                                <b:InvokeCommandAction Command="{Binding DataContext.SelectDocumentCommand, ElementName=Tabs}" 
                            CommandParameter="{Binding}"/>

                            </b:EventTrigger>

                        </b:Interaction.Triggers>

                    </Label>
                </Grid>
            </Border>

        </DataTemplate>
    </Window.Resources>


    <Grid  >
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="{Binding UIModel.GridSettings.TreeWidth, Mode=TwoWay}" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition  Width="{Binding UIModel.GridSettings.AIWidth, Mode=TwoWay}" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ToolBar    Grid.ColumnSpan="5" Background="{StaticResource BackgroundSecondary}" 
                    Padding="8,4" BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderSubtle}">

            <Button   Command="{Binding OpenDirectoryCommand}"  ToolTip="Open Directory">
                <Image Source="/PlantUMLEditor;component/Images/OpenFolder_16x.png"></Image>
            </Button>
            <Separator />
            <Button    Command="{Binding SaveAllCommand}"  ToolTip="Save All">
                <Image Source="/PlantUMLEditor;component/Images/SaveAll_16x.png"></Image>
            </Button>


            <Separator></Separator>
            <ComboBox ItemsSource="{Binding MRUFolders}" MinWidth="100" SelectedItem="{Binding SelectedMRUFolder}">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ComboBoxItem}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,20,0"></TextBlock>
                            <Button Grid.Column="1" Margin="1" Command="{Binding  Path=DataContext.DeleteMRUCommand,  RelativeSource={RelativeSource FindAncestor,
AncestorType={x:Type Window}}}" CommandParameter="{Binding}" FontFamily="Segoe UI">
                                <Button.Content>X</Button.Content>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <Menu HorizontalAlignment="Center" VerticalAlignment="Center">
                <MenuItem Header="Tools"  Margin="0,0,0,0" FontFamily="Segoe UI">
                    <MenuItem Header="Settings">


                        <MenuItem Header="App Settings" Command="{Binding OpenSettingsCommand}"></MenuItem>
                        <MenuItem Header="UML Color Config" Command="{Binding OpenUMLColorConfigCommand}"></MenuItem>
                        <MenuItem Header="Markdown Color Config" Command="{Binding OpenMDColorConfigCommand}"></MenuItem>

                        <Separator>

                        </Separator>
                        <MenuItem Header="Edit Templates" Command="{Binding EditTemplatesCommand}"/>



                    </MenuItem>

                    <MenuItem Header="Scan Directory" Command="{Binding ScanAllFiles}"></MenuItem>
                    <MenuItem Header="Open Terminal" Command="{Binding OpenTerminalCommand}"></MenuItem>

                    <MenuItem Header="Open Explorer" Command="{Binding OpenExplorerCommand}"></MenuItem>

                    <MenuItem Header="Run DOCFx Serve" Command="{Binding DocFXServeCommand}"></MenuItem>

                    <MenuItem Header="Quick GIT Commit and Sync" Command="{Binding GitCommitAndSyncCommand}"></MenuItem>

                </MenuItem>
            </Menu>

            <Separator></Separator>
            <CheckBox IsChecked="{Binding SpellCheck}">Activate Spell Check</CheckBox>
            <Separator></Separator>
            <Button Content="Help" Command="{Binding OpenHelpCommand}" />
            <Separator></Separator>
            <Label Content="Templates:" HorizontalAlignment="Center"  VerticalAlignment="Center"></Label>
            <ComboBox MinWidth="100" SelectedItem="{Binding SelectedTemplate}" IsEnabled="{Binding TemplatesEnabled}" >
                <ComboBox.Resources>
                    <DataTemplate DataType="{x:Type models:TemplateItem}">
                        <TextBlock Text="{Binding Name}"></TextBlock>
                    </DataTemplate>
                    <DataTemplate DataType="System:String">
                        <TextBlock Text="{Binding}"></TextBlock>
                    </DataTemplate>
                    <CollectionViewSource x:Key="ItemsSource" Source="{Binding TemplateStorage.Templates}" />

                </ComboBox.Resources>

                <ComboBox.ItemsSource>
                    <CompositeCollection>

                        <CollectionContainer Collection="{Binding Source={StaticResource ItemsSource}}" />

                    </CompositeCollection>
                </ComboBox.ItemsSource>
            </ComboBox>

            <Button Command="{Binding ApplyTemplateCommand}" Content="Apply Template"></Button>
            <Separator/>
            <Label HorizontalAlignment="Center"  VerticalAlignment="Center" Content="Zoom"></Label>

            <Slider VerticalAlignment="Center" Width="100" Value="{Binding EditorFontSize, UpdateSourceTrigger=PropertyChanged, FallbackValue=14}" Minimum="4" Maximum="32" ></Slider>
        </ToolBar>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*">
                </RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="{Binding UIModel.GridSettings.DataTypesHeight, Mode=TwoWay}"></RowDefinition>
            </Grid.RowDefinitions>
            <TreeView Style="{DynamicResource TreeViewStyle1}"  AllowDrop="True"
                        VirtualizingStackPanel.IsVirtualizing="True"
            VirtualizingStackPanel.VirtualizationMode="Recycling"
                      Grid.Row="0" Grid.RowSpan="1"  ItemsSource="{Binding Folder.Children}">

            </TreeView>
            <GridSplitter Grid.Row="1" Height="5" ShowsPreview="True" HorizontalAlignment="Stretch" Cursor="SizeNS"></GridSplitter>
            <ListView Grid.Row="2"  ItemsSource="{Binding DataTypes}">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="SelectionChanged">
                        <b:CallMethodAction MethodName="GotoDataType" TargetObject="{Binding Path=DataContext,  RelativeSource={RelativeSource FindAncestor,
AncestorType={x:Type ListBox}}}"></b:CallMethodAction>
                    </b:EventTrigger>
                </b:Interaction.Triggers>
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn Width="100" Header="Namespace" DisplayMemberBinding="{Binding DataType.Namespace}"></GridViewColumn>
                            <GridViewColumn Width="Auto" Header="Type" DisplayMemberBinding="{Binding DataType.Name}"></GridViewColumn>
                        </GridView.Columns>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <GridSplitter     HorizontalAlignment="Center" 
              VerticalAlignment="Stretch" Grid.Row="1" Grid.Column="1" ShowsPreview="True" Width="5" Cursor="SizeWE" />
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition   Height="{Binding UIModel.GridSettings.DocumentsWell, Mode=TwoWay}"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <ItemsControl ItemsSource="{Binding OpenDocuments}" ItemTemplateSelector="{StaticResource TabHeaderSelector}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                </ItemsControl>

                <ItemsControl Grid.Row="1" x:Name="Tabs"
                          ItemsSource="{Binding OpenDocuments}"
                          >
                </ItemsControl>
            </Grid>
            <GridSplitter Grid.Row="1" ShowsPreview="True"  Height="5" ResizeDirection="Rows" HorizontalAlignment="Stretch" Cursor="SizeNS"></GridSplitter>
            <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedToolTab, Mode=TwoWay}" Background="White" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,1,0,0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Grid>
                                        <Border Name="Border" Background="{StaticResource BackgroundSecondary}" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,1,0" Padding="12,8">
                                            <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" Margin="0" RecognizesAccessKey="True" />
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="White" />
                                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryAccent}" />
                                            <Setter TargetName="Border" Property="BorderThickness" Value="0,0,1,3" />
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryAccent}" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="{StaticResource BackgroundHover}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="FontFamily" Value="Segoe UI" />
                        <Setter Property="FontSize" Value="12" />
                    </Style>
                </TabControl.Resources>
                <TabItem>
                    <TabItem.Header>Errors</TabItem.Header>
                    <TabItem.Style>
                        <Style TargetType="TabItem">
                            <Setter Property="Foreground" Value="Red"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Messages.Count}" Value="0">
                                    <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TabItem.Style>
                    <DataGrid SelectionMode="Single" IsReadOnly="True"
                              Grid.Row="2"
                              AutoGenerateColumns="False"  ItemsSource="{Binding Source={StaticResource GroupedMessages}}" Margin="0"
                           HorizontalContentAlignment="Stretch"
                                 SelectedItem="{Binding SelectedMessage}">
                        <DataGrid.Resources>
                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#E8F4FD"/>
                        </DataGrid.Resources>
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" />
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="createMissingMethod" Command="{Binding FixingCommand}"   CommandParameter="{Binding}"
                                Content="Fix" Grid.Column="2"  />
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsFixable}" Value="false">
                                                <DataTrigger.Setters>
                                                    <Setter TargetName="createMissingMethod" Property="Visibility" Value="Collapsed"></Setter>
                                                </DataTrigger.Setters>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Text"  IsReadOnly="true"  >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center"  Text="{Binding Text}"></TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Line"  IsReadOnly="true"  >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center"  Text="{Binding LineNumber}"></TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTemplateColumn Header="File"  IsReadOnly="true"  >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Center"  Text="{Binding RelativeFileName}"></TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem>
                    <TabItem.Header>Find In Files</TabItem.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition Width="100"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <TextBox Margin="2,2,5,2"  x:Name="txtGF">
                            <b:Interaction.Triggers>
                                <b:KeyTrigger Key="Return" ActiveOnFocus="True">

                                    <b:InvokeCommandAction   CommandParameter="{Binding Text, ElementName=txtGF}"  Command="{Binding  GlobalSearchCommand}"></b:InvokeCommandAction>
                                </b:KeyTrigger>
                            </b:Interaction.Triggers>
                        </TextBox>

                        <Button Margin="2,2,2,2"  Grid.Column="1" Command="{Binding GlobalSearchCommand}"
                                CommandParameter="{Binding Text, ElementName=txtGF}" >Find</Button>
                        <DataGrid SelectionMode="Single"  AutoGenerateColumns="False" 
                                  ItemsSource="{Binding GlobalFindResults}" 
                                  Margin="{DynamicResource TabHeaderMargin}"
                                  Grid.Row="1" Grid.ColumnSpan="3" HorizontalContentAlignment="Stretch"
                                 SelectedItem="{Binding SelectedGlobalFindResult}">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Cyan"/>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn IsReadOnly="true" Header="File" Binding="{Binding FileName}"></DataGridTextColumn>
                                <DataGridTextColumn IsReadOnly="true" Header="LineNumber" Binding="{Binding LineNumber}"></DataGridTextColumn>
                                <DataGridTextColumn IsReadOnly="true" Header="Text" Binding="{Binding Text}"></DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>
                <TabItem>
                    <TabItem.Header>Find References</TabItem.Header>
                    <Grid>
                        <DataGrid SelectionMode="Single"  AutoGenerateColumns="False" 
                                  ItemsSource="{Binding FindReferenceResults}" 
                                  Margin="{DynamicResource TabHeaderMargin}"
                                  Grid.Row="1" Grid.ColumnSpan="3" HorizontalContentAlignment="Stretch"
                                 SelectedItem="{Binding SelectedGlobalFindResult}">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Cyan"/>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn IsReadOnly="true" Header="Text" Binding="{Binding Text}"></DataGridTextColumn>
                                <DataGridTextColumn IsReadOnly="true" Header="LineNumber" Binding="{Binding LineNumber}"></DataGridTextColumn>

                                <DataGridTextColumn IsReadOnly="true" Header="File" Binding="{Binding FileName}"></DataGridTextColumn>

                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>
                <TabItem>
                    <TabItem.Header>
                        GIT
                    </TabItem.Header>
                    <Grid>

                        <TextBox Grid.Row="1" Text="{Binding GitMessages}"></TextBox>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>

        <GridSplitter ResizeBehavior="PreviousAndNext" Grid.Column="3" Grid.Row="1" Width="5" ShowsPreview="True" ResizeDirection="Columns" HorizontalAlignment="Center" 
                     VerticalAlignment="Stretch" Cursor="SizeWE"></GridSplitter>
        <Grid Grid.Column="4" Grid.Row="1" >


            <Grid Grid.Column="3" Grid.RowSpan="3">
                <Grid.ColumnDefinitions>

                    <ColumnDefinition Width="1*"></ColumnDefinition>
                 
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                     <ScrollViewer  
             
HorizontalScrollBarVisibility="Disabled"
VerticalScrollBarVisibility="Auto" x:Name="ChatScroll"
CanContentScroll="False">
                    <b:Interaction.Triggers>


                        <b:EventTrigger EventName="CollectionChanged" SourceObject="{Binding AIConversation}">
                            <b:CallMethodAction MethodName="ScrollToBottom" TargetObject="{Binding ElementName=ChatScroll}" />
                        </b:EventTrigger>
                    </b:Interaction.Triggers>

                    <ItemsControl ItemsSource="{Binding AIConversation}">
                        <ItemsControl.ItemTemplate>

                            <DataTemplate>
                                <Border BorderThickness="1" CornerRadius="5" Padding="4" BorderBrush="Gray" Margin="5"   >
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                                    <DataTrigger.Setters>
                                                        <Setter   Property="Background" Value="LightBlue">
                                                        </Setter>
                                                        <Setter   Property="HorizontalAlignment" Value="Right"/>
                                                    </DataTrigger.Setters>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsUser}" Value="False">
                                                    <DataTrigger.Setters>
                                                        <Setter   Property="Background" Value="WhiteSmoke">
                                                        </Setter>
                                                    </DataTrigger.Setters>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Vertical">

                                        <Expander Header="Tool Calls" ExpandDirection="Down"  IsExpanded="False" >
                                            <Expander.Style>

                                                <Style TargetType="Expander">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ToolCalls.Count}" Value="0">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Expander.Style>
                                            <ListBox ItemsSource="{Binding ToolCalls}">
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <StackPanel Orientation="Vertical">
                                                            <TextBlock Text="{Binding ToolName}" FontWeight="Bold"></TextBlock>
                                                            <TextBlock Text="{Binding Arguments}"></TextBlock>

                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </Expander>
                                        <Expander Header="Undo Operations"  ExpandDirection="Down">

                                            <Expander.Style>
                                                <Style TargetType="Expander">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Undos.Count}" Value="0">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Expander.Style>
                                            <Grid >
                                                <Grid.RowDefinitions>
                                                    <RowDefinition></RowDefinition>
                                                    <RowDefinition></RowDefinition>
                                                </Grid.RowDefinitions>

                                                <ListBox ItemsSource="{Binding Undos}">
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <StackPanel Orientation="Vertical">
                                                                <TextBlock Text="{Binding undoType}" FontWeight="Bold"></TextBlock>
                                                                <TextBlock Text="{Binding fileName}"></TextBlock>
                                                                <TextBlock Text="{Binding textBefore}"></TextBlock>


                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                </ListBox>
                                                <Button Grid.Row="1"  CommandParameter="{Binding Undos}" Command="{Binding DataContext.UndoAIEditsCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}, AncestorLevel=1}}">Undo Operations</Button>
                                            </Grid>
                                        </Expander>
                                        <TextBlock Foreground="Green">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBusy}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                          Running
                                        </TextBlock>

                                        <FlowDocumentScrollViewer ScrollViewer.VerticalScrollBarVisibility="Disabled" Padding="5" Document="{Binding Document}"  >
                                            <b:Interaction.Behaviors>
                                                <behaviors:BubbleMouseWheelToChatScrollBehavior />
                                            </b:Interaction.Behaviors>
                                        </FlowDocumentScrollViewer>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>

                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <Border Grid.Row="1"  BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderSubtle}" Background="{StaticResource BackgroundSecondary}" Padding="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="New Chat" Margin="0,0,10,0"   Command="{Binding NewChatCommand}"></Button>

                        <TextBox Grid.Column="1" FontFamily="Segoe UI" FontSize="12" AcceptsReturn="False" AcceptsTab="True"
                                 Text="{Binding ChatText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10,8" MinHeight="40" MaxHeight="100"
                                 VerticalScrollBarVisibility="Auto"
                                 BorderThickness="1" BorderBrush="{StaticResource BorderSubtle}"
                                 Background="{StaticResource BackgroundMain}">
                            <b:Interaction.Triggers>
                                <b:KeyTrigger Key="Return" ActiveOnFocus="True">
                                    <b:InvokeCommandAction Command="{Binding SendChatCommand}"  CommandParameter="{Binding CurrentDocument}" />
                                </b:KeyTrigger>
                            </b:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="2" Content="Send" Command="{Binding SendChatCommand}"  CommandParameter="{Binding CurrentDocument}"
                                Padding="14,10" MinWidth="80" Height="40" Margin="8,0,0,0"
                                Background="{StaticResource PrimaryAccent}" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <StatusBar Grid.Row="2" Grid.ColumnSpan="5" Background="{StaticResource BackgroundSecondary}"
                   BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderSubtle}" 
                   Padding="8,4"  >
            <StatusBarItem>
                <Button Command="{Binding CancelExecutingAction}">Cancel</Button>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding CurrentActionExecuting}"></TextBlock>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Branch: "></Run>
                    <Run Text="{Binding CurrentGitBranch}" />
                </TextBlock>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Repo: "></Run>
                    <Run Text="{Binding CurrentGitRepoRoot}" />
                </TextBlock>
            </StatusBarItem>
        </StatusBar>

        <Popup IsOpen="{Binding ConfirmOpen}" AllowsTransparency="True" Width="Auto" Height="Auto" HorizontalAlignment="Center" VerticalAlignment="Center"   Placement="Center"  >
            <Grid Margin="10,10,10,10">



                <Border BorderThickness="1" CornerRadius="8" BorderBrush="{StaticResource BorderSubtle}" Background="White">
                    <Border.Effect>
                        <DropShadowEffect Direction="315"  ShadowDepth="8" BlurRadius="16"  Opacity="0.3" />
                    </Border.Effect>
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition></RowDefinition>
                            <RowDefinition></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition></ColumnDefinition>
                            <ColumnDefinition></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.ColumnSpan="2" Background="White" Margin="8" FontSize="14" TextWrapping="Wrap">You have unsaved changes. Continue closing?</TextBlock>
                        <Button Grid.Row="1" Margin="8,12,4,8" Padding="16,8">
                            <Button.Content>Yes</Button.Content>

                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="Click">

                                    <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="AllowContinueClosing" Value="true"></b:ChangePropertyAction>
                                    <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="ConfirmOpen" Value="false"></b:ChangePropertyAction>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </Button>
                        <Button Grid.Row="1" Grid.Column="1" Margin="4,12,8,8" Padding="16,8">
                            <Button.Content>No</Button.Content>

                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="Click">

                                    <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="AllowContinueClosing" Value="false"></b:ChangePropertyAction>
                                    <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="ConfirmOpen" Value="false"></b:ChangePropertyAction>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </Button>
                    </Grid>
                </Border>
            </Grid>

        </Popup>
    </Grid>
</Window>